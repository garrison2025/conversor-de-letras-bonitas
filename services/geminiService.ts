// AI Functionality Disabled
// This file is intentionally left empty/placeholder.
// If you need AI features back, restore the gemini code and the import map in index.html.

import { FontCategory } from '../types';

export const generateCreativeText = async (
  text: string, 
  category: FontCategory
): Promise<string[]> => {
    // Stub return to satisfy potential imports if any remain, though they shouldn't.
    console.warn("AI generation is disabled.");
    return [];
};
